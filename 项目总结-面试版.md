#  hm-dianping（黑马点评）项目总结 — 面试版

> 本项目基于黑马点评二次开发，在原有秒杀、社交功能基础上，**自主设计并实现了私聊系统和 AI Agent 智能助手**两个核心模块，共计约 1000+ 行新增代码。

---

## 一、项目整体介绍

**项目名称**：hm-dianping（探店笔记平台）

**技术栈**：
| 类别 | 技术 |
|------|------|
| 后端框架 | Spring Boot 2.3.12 + MyBatis-Plus 3.5.3 |
| 数据库 | MySQL 8.0 + Redis（Lettuce连接池） |
| 分布式锁 | Redisson 3.17.5 |
| 实时通信 | Spring WebSocket + STOMP + SockJS |
| AI 服务 | 阿里云 DashScope（通义千问 qwen-max）+ OkHttp |
| 工具库 | Hutool 5.7 + Lombok + AOP |

**核心功能模块**：
1. **用户系统**：手机验证码登录、Redis分布式Session、签到（BitMap）
2. **商户系统**：商铺缓存（穿透/击穿防护）、GEO附近搜索
3. **秒杀系统**：Lua脚本原子校验 → Redis Stream异步下单 → 乐观锁扣库
4. **社交系统**：点赞排行（SortedSet）、关注/取关（Set）、Feed流推模式
5. **🆕 私聊系统**：WebSocket点对点消息、未读计数、会话管理
6. **🆕 AI Agent 智能助手**：自实现Function Calling循环、店铺查询工具链

**Redis 使用全景**（13个场景）：

| Redis Key | 数据结构 | 用途 |
|-----------|---------|------|
| `login:code:{phone}` | String | 短信验证码（2分钟TTL） |
| `login:token:{uuid}` | Hash | 用户Session（25小时TTL） |
| `cache:shop:{id}` | String(JSON) | 商铺缓存（空值防穿透，30分钟TTL） |
| `cache:shop:type:*` | String(JSON) | 店铺类型缓存 |
| `lock:shop:{id}` | String | 缓存重建互斥锁 |
| `seckill:stock:{vid}` | String | 秒杀库存 |
| `seckill:order:{vid}` | Set | 已购用户集合（一人一单） |
| `stream.orders` | Stream | 异步下单消息队列 |
| `blog:liked:{blogId}` | SortedSet | 点赞排行（score=时间戳） |
| `feed:{userId}` | SortedSet | Feed推文流（score=时间戳） |
| `shop:geo:{typeId}` | GEO | 商铺地理位置 |
| `sign:{userId}` | BitMap | 用户签到 |
| `user:follow:{userId}` | Set | 关注列表 |
| `chat:unread:{toId}:{fromId}` | String(int) | 未读消息计数 |

---

## 二、私聊系统（重点模块 ①）

### 2.1 整体架构

```
┌──────────┐   HTTP POST /message/send   ┌──────────────────┐
│  前端A   │ ──────────────────────────→  │  MessageController│
│          │                              │                  │
│  前端B   │   WebSocket /api/ws/chat     │  MessageService  │
│  (SockJS │ ←────────────────────────── │  (消息存储+推送)  │
│  +STOMP) │   convertAndSendToUser()     │                  │
└──────────┘                              └───────┬──────────┘
                                                  │
                                    ┌─────────────┼─────────────┐
                                    ▼             ▼             ▼
                              ┌──────────┐  ┌─────────┐  ┌──────────┐
                              │  MySQL   │  │  Redis   │  │WebSocket │
                              │ tb_msg   │  │ 未读计数  │  │  Broker  │
                              │ tb_conv  │  │          │  │  /queue  │
                              └──────────┘  └─────────┘  └──────────┘
```

### 2.2 技术选型与设计思路

**为什么选 WebSocket + STOMP？**
- HTTP 是请求-响应模型，无法实现服务端主动推送，轮询方案延迟高且浪费资源
- WebSocket 提供全双工通信，服务端可主动推送消息到客户端
- STOMP 协议在 WebSocket 之上提供了发布/订阅的语义抽象，简化点对点消息路由
- SockJS 作为降级方案，兼容不支持 WebSocket 的浏览器

**WebSocket 鉴权如何实现？**
- WebSocket 握手阶段不走 HTTP Header（无法携带 Authorization），所以通过 **URL 参数** 传递 token
- 自定义 `HandshakeInterceptor`（`WebSocketAuthInterceptor`）：从 URL 取 token → Redis 查用户 → userId 存入 WebSocket Session 属性
- STOMP CONNECT 阶段，通过 `ChannelInterceptor` 读取 Session 属性中的 userId，包装为 `StompPrincipal` 绑定到 STOPM 连接上

### 2.3 核心流程

**发送消息**：
```
1. Controller 接收 toUserId + content
2. ConversationService.getOrCreateConversation(fromId, toId)
   → 双向查询（A→B 或 B→A），不存在则创建
3. Message 入库（conversationId, fromUserId, toUserId, content）
4. Redis INCR 未读计数：chat:unread:{toUserId}:{fromUserId}，设30天TTL
5. SimpMessagingTemplate.convertAndSendToUser(toUserId, "/queue/messages", MessageDTO)
   → 通过 STOMP Broker 推送到目标用户的 WebSocket 连接
```

**查询消息列表**：
```
1. 校验当前用户是否为会话成员（防越权）
2. 查询会话下所有消息
3. 批量查用户信息（N+1优化）：收集所有 fromUserId → listByIds → Map<Long, User>
4. 组装 MessageDTO（content, isSelf, fromNickname, fromIcon）
5. 清除未读计数：Redis DEL chat:unread:{currentUserId}:{otherUserId}
```

**查询会话列表**：
```
1. 按 lastTime DESC 查询用户参与的所有会话
2. 对每个会话：判断对方是 userA 还是 userB → 查对方用户信息
3. 从 Redis 获取未读数：GET chat:unread:{myId}:{otherId}
```

### 2.4 技术难点与解决方案

**难点 1：WebSocket 鉴权与用户身份绑定**

> 问题：WebSocket 握手后，STOMP 层如何知道当前连接是哪个用户？只有知道用户身份，才能实现 `convertAndSendToUser()` 的精准推送。

> 解决方案：**两层拦截器**。
> - 第一层 `WebSocketAuthInterceptor`（握手阶段）：从 URL 参数取 token → Redis 查登录信息 → userId 存入 `SessionAttributes`
> - 第二层 `ChannelInterceptor`（STOMP CONNECT 阶段）：从 `SessionAttributes` 读 userId → 创建 `StompPrincipal(userId)` → 绑定到 STOMP 连接
> 
> 这样 `SimpMessagingTemplate.convertAndSendToUser(userId, ...)` 才能根据 Principal 找到目标连接并推送。

**难点 2：会话模型的双向查询**

> 问题：A 给 B 发消息创建了一个会话(userA=A, userB=B)，之后 B 再给 A 发消息应该复用同一会话，而不是创建新会话。

> 解决方案：`getOrCreateConversation()` 方法使用 **双向 OR 查询**：
> ```sql
> WHERE (user_a_id = ? AND user_b_id = ?) OR (user_a_id = ? AND user_b_id = ?)
> ```
> 查不到才创建，保证同一对用户永远只有一个会话。

**难点 3：消息列表的 N+1 查询**

> 问题：查询一个会话下的所有消息时，每条消息都需要查发送者的昵称和头像，如果逐条查询用户表会导致 N+1 问题。

> 解决方案：**批量查询 + Map 映射**。
> ```java
> Set<Long> userIds = messages.stream().map(Message::getFromUserId).collect(Collectors.toSet());
> Map<Long, User> userMap = userService.listByIds(userIds).stream()
>     .collect(Collectors.toMap(User::getId, u -> u));
> ```
> 先收集所有发送者ID去重，一次批量查询，再通过 Map 组装 DTO。无论多少条消息，只查一次用户表。

**难点 4：未读消息计数的原子性与清理**

> 问题：多个用户同时给同一个人发消息，未读数需要准确递增；用户查看消息后需要正确清零。

> 解决方案：
> - 使用 Redis `INCR` 命令，天然原子性，不需要加锁
> - Key 设计为 `chat:unread:{toUserId}:{fromUserId}`，粒度为"某人对我的未读数"
> - 用户打开某个会话时，`DEL` 该 Key 清零（不使用 DECR，避免并发问题）
> - 设置 30 天 TTL，避免 Redis 内存泄漏

### 2.5 面试可能追问

| 追问 | 回答要点 |
|------|---------|
| 为什么不用长轮询？ | 长轮询本质还是HTTP，每次请求都带完整Header，并发高时连接开销大；WebSocket是真正的全双工，一次握手持续通信，服务端可主动推 |
| STOMP 和原生 WebSocket 的区别？ | 原生WebSocket需要自己实现消息路由、订阅管理；STOMP提供 `SUBSCRIBE`/`SEND`/`MESSAGE` 等语义，Spring内部帮你管理订阅关系 |
| SockJS 的作用？ | WebSocket降级方案：优先用WebSocket，不支持时自动降级为 xhr-streaming/xhr-polling 等 |
| 离线消息怎么处理？ | 当前方案消息入库MySQL，用户上线后通过HTTP查询历史消息获取离线内容；未读数存在Redis中，上线后可展示未读角标 |
| 消息的安全性？ | 发送消息校验登录态（ThreadLocal）；查询消息校验会话归属权；删除消息校验发送者或接收者身份 |

---

## 三、AI Agent 智能助手（重点模块 ②）

### 3.1 整体架构

```
┌──────────┐   POST /ai/agent   ┌──────────────────────┐
│  前端     │ ─────────────────→ │    AiController      │
│          │ ←───────────────── │    AiServiceImpl     │
│  展示     │  AgentReply DTO    │                      │
│  文字+卡片│  {text, shops[]}   │  ┌────────────────┐  │
└──────────┘                    │  │ Function Calling │  │
                                │  │ Loop (max 5次)   │  │
                                │  │                  │  │
                                │  │  ┌────────────┐ │  │
                                │  │  │ DashScope  │ │  │
                                │  │  │ qwen-max   │ │  │
                                │  │  └─────┬──────┘ │  │
                                │  │        │         │  │
                                │  │  ┌─────▼──────┐ │  │
                                │  │  │finish_reason│ │  │
                                │  │  │= tool_calls │ │  │
                                │  │  └─────┬──────┘ │  │
                                │  │        │         │  │
                                │  │  ┌─────▼──────┐ │  │
                                │  │  │executeTool │ │  │
                                │  │  │searchShop  │ │  │
                                │  │  │getVoucher  │ │  │
                                │  │  │getShopDetail│ │  │
                                │  │  └─────┬──────┘ │  │
                                │  └────────┼────────┘  │
                                │           ▼            │
                                │  ┌────────────────┐   │
                                │  │  MySQL / Redis │   │
                                │  │  (业务数据查询) │   │
                                │  └────────────────┘   │
                                └──────────────────────┘
```

### 3.2 设计思路

**为什么自己实现 Function Calling 而不用 Spring AI？**

- Spring AI 框架当时功能还在快速迭代中，版本不稳定
- 自实现可以更精细地控制 Agent 的行为：循环次数、工具结果的收集方式、错误处理
- 面试时更能体现对底层原理的理解，而不是"我会用框架"
- Function Calling 本质就是一个循环：调模型 → 判断 finish_reason → 执行工具 → 把结果喂回去 → 再调模型

**核心数据流**：

```
用户输入 "附近有什么好吃的火锅店"
    │
    ▼
[第1次] 调用 qwen-max，携带 tools 定义
    │  finish_reason = "tool_calls"
    │  → tool_call: searchShop(keyword="火锅")
    │
    ▼
executeTool("searchShop", '{"keyword":"火锅"}')
    │  → MyBatisPlus: LIKE查询 tb_shop，limit 5
    │  → 返回 JSON: [{id:1, name:"海底捞", ...}, ...]
    │
    ▼  collectShopCards() → 收集到 collectedShopMap
[第2次] 把工具结果追加到 messages，再调 qwen-max
    │  finish_reason = "tool_calls"
    │  → tool_call: getVoucher(shopId=1)
    │
    ▼
executeTool("getVoucher", '{"shopId":1}')
    │  → 查询该店铺的优惠券信息
    │
    ▼  collectShopCards() → 合并到 collectedShopMap
[第3次] 再调 qwen-max
    │  finish_reason = "stop"（模型认为信息够了，生成最终回复）
    │  → content: "为你找到3家火锅店推荐..."
    │
    ▼
返回 AgentReply { text: "为你找到...", shops: [ShopCard, ShopCard, ShopCard] }
前端渲染：文字 + 店铺卡片列表
```

### 3.3 核心实现细节

**Function Calling 循环**（`agentChat` 方法，核心约 60 行）：

```java
Map<Long, AgentReply.ShopCard> collectedShopMap = new LinkedHashMap<>(); // 有序去重
List<Map<String, Object>> messages = buildRequestMessages(systemPrompt, history);
List<Map<String, Object>> tools = buildTools();

for (int i = 0; i < 5; i++) {  // 最多5轮工具调用，防止死循环
    String responseBody = callModel(buildChatBody(messages, tools));
    Map<String, Object> result = objectMapper.readValue(responseBody, Map.class);
    // ... 解析 output.choices[0]
    String finishReason = choice.get("finish_reason");
    Map<String, Object> assistantMsg = choice.get("message");
    messages.add(assistantMsg);  // 追加到上下文

    if ("stop".equals(finishReason)) {
        // 模型生成最终回复，退出循环
        String finalAiText = assistantMsg.get("content");
        List<AgentReply.ShopCard> shops = new ArrayList<>(collectedShopMap.values());
        saveAssistantMessage(userId, finalAiText, shops);
        return Result.ok(new AgentReply(finalAiText, shops));
    }

    if ("tool_calls".equals(finishReason)) {
        // 执行每个工具调用
        for (toolCall : toolCalls) {
            String toolResult = executeTool(toolName, arguments);
            collectShopCards(toolName, toolResult, collectedShopMap);  // 收集店铺卡片
            messages.add(toolResultMsg);  // 工具结果追加到上下文
        }
    }
}
return Result.fail("Agent处理超时，请简化你的问题");
```

**三个注册工具**：

| 工具名 | 描述 | 实现方式 |
|--------|------|---------|
| `searchShop` | 根据关键词搜索店铺 | `shopService.query().like("name", keyword).last("limit 5").list()` |
| `getVoucher` | 查询店铺优惠券 | `voucherService.queryVoucherOfShop(shopId)` |
| `getShopDetail` | 查询店铺详情 | `shopService.getById(shopId)` |

**动态 System Prompt**（`@PostConstruct` 初始化）：

```java
@PostConstruct
public void init() {
    // 项目启动时从数据库加载所有店铺类型
    List<String> shopTypes = shopTypeService.list().stream()
            .map(shopType -> shopType.getName())
            .collect(Collectors.toList());
    shopTypePrompt = "平台支持的店铺类型包括：" 
                   + String.join("、", shopTypes) 
                   + "。当用户询问店铺推荐时，应根据用户需求匹配相应的类型，并调用searchShop工具查询。";
}
```

这样做的好处是：**系统提示词随数据库动态变化**，新增店铺类型后无需改代码重启服务。

**Agent 回复的持久化设计**（核心难点之一）：

> 问题：Agent 回复包含两部分：文字描述 + 结构化的店铺卡片数据。数据库 `tb_ai_message.content` 是一个 VARCHAR 字段，如何同时存储这两种数据？

> 解决方案：**前缀编码协议** `__AGENT_REPLY__`。

```java
// 编码（保存时）：text + shops → JSON → 加前缀
private String encodeAssistantContent(String text, List<ShopCard> shops) {
    AgentReply payload = new AgentReply(text, shops);
    return "__AGENT_REPLY__" + objectMapper.writeValueAsString(payload);
}

// 解码（读取历史时）：判断前缀 → 截取JSON → 反序列化
private AiMessageDTO toHistoryMessage(AiMessage msg) {
    if (msg.getContent().startsWith("__AGENT_REPLY__")) {
        String json = msg.getContent().substring("__AGENT_REPLY__".length());
        AgentReply payload = objectMapper.readValue(json, AgentReply.class);
        dto.setContent(payload.getText());
        dto.setShops(payload.getShops());
    }
    return dto;
}
```

好处：
- 普通聊天消息直接存纯文本，不受影响
- Agent 消息通过前缀区分，解析时才反序列化
- 不需要额外字段或新表，扩展性好

**多轮工具调用的店铺卡片去重合并**：

> 问题：模型可能在第1轮调用 `searchShop("火锅")` 返回了海底捞，第2轮又调用 `getShopDetail(1)` 也返回了海底捞，如果不去重，前端会显示重复卡片。

> 解决方案：使用 `LinkedHashMap<Long, ShopCard>`，以 shopId 为 key：
> - `LinkedHashMap` 保证插入顺序
> - 同一个 shopId 后续调用会 **合并**（merge）而非覆盖，比如 searchShop 返回基础信息，getVoucher 返回优惠券信息，两者合并为完整卡片

### 3.4 技术难点与解决方案

**难点 1：Function Calling 循环控制**

> 问题：模型可能在某些边界情况下反复调用工具形成死循环，导致请求超时、API费用暴涨。

> 解决方案：
> - 硬性限制最多 5 轮工具调用
> - 超过限制直接返回失败提示"请简化你的问题"
> - 每次工具调用的结果都追加到 messages 上下文中，帮助模型判断"信息已经够了"

**难点 2：大模型返回结构不确定**

> 问题：DashScope API 返回的是嵌套的 Map 结构，没有强类型，字段可能缺失或为 null。

> 解决方案：
> - 编写 `castMap()` 和 `castListOfMap()` 两个工具方法，安全地进行类型转换
> - 每个关键节点都做 null 判断
> - 工具执行用 try-catch 包裹，返回 `{"error":"..."}` 而非抛异常，让模型能根据错误信息调整策略

**难点 3：工具结果与卡片数据的收集**

> 问题：模型一次可能调用多个工具（parallel tool calls），每个工具返回的 JSON 结构不同，需要统一收集为 ShopCard。

> 解决方案：
> - `collectShopCards()` 方法根据 toolName 分发处理
> - `mergeShopCard()` 方法：用 shopId 做 key，已存在的卡片只更新非空字段（保护已有数据不被 null 覆盖）

**难点 4：Agent 回复中文字与卡片的混合存储与展示**

> 问题：一条消息需要同时包含文字描述和结构化的店铺卡片，前端需要分别渲染。

> 解决方案（前缀编码协议，如上 3.3 所述）：
> - 后端存储：`__AGENT_REPLY__` + JSON(`{text, shops[]}`)
> - 前端接收 `AgentReply` DTO，文字直接渲染，shops 渲染为卡片列表
> - 历史消息加载时解码还原，体验一致

### 3.5 面试可能追问

| 追问 | 回答要点 |
|------|---------|
| Function Calling 的原理？ | 大模型生成特殊的 `finish_reason: "tool_calls"` 而非 `"stop"`，附带工具名和参数JSON；后端执行工具后把结果以 `role: "tool"` 追加回上下文，再调模型让它继续 |
| 为什么限制5轮？ | 经验值。一般1-3轮足够（查店+查券），5轮已是上限。防止边界情况死循环，也控制 API 成本 |
| 模型幻觉怎么处理？ | System Prompt 明确要求"调用工具查询真实数据，不要凭空编造"；工具描述中写明参数和用途；工具结果直接来自数据库，保证真实性 |
| 动态 System Prompt 有什么好处？ | 店铺类型随数据库动态更新，新增类型无需改代码；模型知道平台有什么类型，减少无效调用 |
| 如何保证 API 调用的可靠性？ | OkHttp 设置连接超时30s、读取超时60s；异常时返回友好错误提示；工具执行失败不会抛异常，而是返回 error JSON 让模型自行调整 |
| 如果要新增一个工具怎么做？ | `buildTools()` 中新增一个 Map 定义；`executeTool()` 中新增一个 if 分支；符合开闭原则 |

---

## 四、其他核心模块要点

### 4.1 秒杀系统（三层演进）

| 版本 | 方案 | 问题 |
|------|------|------|
| V1 | 同步：查DB库存 → 加锁 → 下单 | 并发性能差，锁竞争严重 |
| V2 | Lua脚本校验 + BlockingQueue异步下单 | JVM内存队列，宕机丢数据 |
| V3（当前） | **Lua脚本校验 + Redis Stream异步下单** | 持久化不丢数据，消费者组+ACK保证可靠消费 |

**Lua 脚本原子操作**（`seckill.lua`）：
```
判断库存 → 判断一人一单 → 扣库存 → 记录已购用户 → 发送Stream消息
```
全部在一个 Lua 脚本中原子执行，不存在并发问题。

**消费者可靠性**：
- `@PostConstruct` 启动单线程消费 `VoucherOrderHandler`
- 正常流程：读消息 → 创建订单 → ACK确认
- 异常流程：处理 PendingList 中未 ACK 的消息，确保不丢失

### 4.2 分布式锁演进

| 阶段 | 实现 | 特点 |
|------|------|------|
| V1 | `SETNX` + 固定TTL | 释放可能误删别人的锁 |
| V2 | `SETNX` + UUID标识 + Lua释放 | 保证只释放自己的锁 |
| V3（Redisson） | 可重入锁 + 看门狗自动续期 | 生产级方案 |

### 4.3 用户认证（双拦截器）

| 拦截器 | Order | 职责 |
|--------|-------|------|
| `RefreshTokenInterceptor` | 0 | 拦截所有路径，从 Header 取 token → Redis 查用户 → ThreadLocal → **刷新 TTL** |
| `LoginInterceptor` | 1 | 仅拦截需登录的路径（排除 `/shop/**`、`/user/login` 等公开接口），检查 ThreadLocal |

**设计亮点**：RefreshToken 拦截所有请求，确保已登录用户访问任何页面都能续期 token；LoginInterceptor 只负责鉴权判断。`afterCompletion` 中清除 ThreadLocal 防止内存泄漏。

### 4.4 全局唯一 ID 生成器

```
64位 Long = 时间戳(32位) + 序列号(32位)
```
- 时间戳 = 当前秒 - 基准时间（2022-01-01）
- 序列号 = Redis `INCR icr:{业务前缀}:{日期}`
- 按天重置序列号，单天支持 2^32 ≈ 42亿条

---

## 五、项目亮点总结

1. **AI Agent 自实现 Function Calling**：不依赖 Spring AI，手动实现多轮工具调用循环、动态 System Prompt、前缀编码协议实现混合内容持久化、LinkedHashMap 多轮卡片去重合并
2. **WebSocket 私聊系统**：STOMP + SockJS 实现点对点消息推送，双层拦截器完成 WebSocket 鉴权与用户身份绑定
3. **Redis 深度使用**：13 个场景覆盖 String / Hash / Set / ZSet / BitMap / GEO / Stream 七大数据结构
4. **秒杀系统渐进式优化**：从同步到内存队列到 Redis Stream，体现架构演进思维
5. **分布式锁三阶段演进**：SETNX → Lua 原子释放 → Redisson，理解锁的本质

---

## 六、STAR 法则话术示例

### 示例 1：AI Agent 功能

**S（情境）**：平台需要一个智能助手，用户可以用自然语言问"附近有什么好吃的火锅店"，系统应该从数据库查真实数据返回，而不是让 AI 编造。

**T（任务）**：我负责设计并实现 AI Agent 智能助手模块，要求接入大模型 API，实现 Function Calling 让模型能调用后端工具查询真实数据。

**A（行动）**：
1. 选型：不用 Spring AI（版本不稳定），用 OkHttp 直接调用阿里云 DashScope API
2. 核心设计：实现了 Function Calling 循环——每次调模型后判断 `finish_reason`，是 `tool_calls` 就执行工具并把结果喂回去，是 `stop` 就返回最终回复，最多循环 5 次
3. 注册了 3 个工具：searchShop（LIKE 查询）、getVoucher（查优惠券）、getShopDetail（查详情）
4. 解决了混合内容持久化问题：用 `__AGENT_REPLY__` 前缀 + JSON 编码的方式，在一个字段中同时存储文字和店铺卡片数据
5. 用 `LinkedHashMap<shopId, ShopCard>` 实现多轮调用中的卡片去重和字段合并

**R（结果）**：用户可以用自然语言查询店铺和优惠券，AI 返回文字+卡片组合的富媒体回复，历史消息可以正确还原展示。

### 示例 2：私聊系统

**S（情境）**：平台需要用户之间的即时通讯功能，支持实时消息推送、未读消息计数、会话管理。

**T（任务）**：我负责从零设计和实现完整的私聊系统。

**A（行动）**：
1. 技术选型：Spring WebSocket + STOMP 协议 + SockJS 降级
2. 鉴权方案：自定义 `HandshakeInterceptor` 从 URL 参数取 token 鉴权，`ChannelInterceptor` 在 STOMP CONNECT 时绑定 `StompPrincipal` 实现用户身份关联
3. 未读计数：用 Redis `INCR` 原子递增，Key 粒度为"某人对我的未读"，查看会话时 `DEL` 清零
4. 性能优化：消息列表查询用 `listByIds` 批量查用户信息，避免 N+1 问题
5. 会话去重：`getOrCreateConversation` 双向 OR 查询，保证同一对用户只有一个会话

**R（结果）**：实现了完整的即时通讯功能，消息实时推送延迟 < 100ms，未读计数准确无误。
